# ⚠️ NOTE: Notez que si les variables d'environnement ne sont pas définies,
# Symfony génèrera une erreur lors de l'accès à la base de données.

# ======= Base dev ========================================================
DATABASE_URL=${DB_MS}://${MARIADB_USER}:${MARIADB_PASSWORD}@${DB_HOST}:${DB_PORT}/${MARIADB_DATABASE}
# =========================================================================
